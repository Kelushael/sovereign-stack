{
  "_comment": "Add this to ~/.config/claude/claude_desktop_config.json (macOS: ~/Library/Application Support/Claude/claude_desktop_config.json)",
  "mcpServers": {
    "axis": {
      "command": "axis",
      "args": ["--mcp"],
      "_note": "axis must be on PATH. Run: bash install.sh first.",
      "_description": "Sovereign AI CLI — replaces Desktop Commander MCP. Runs on YOUR node, zero cloud."
    }
  }
}
